# API Design

## Table of Contents

- [API Standards](#api-standards)
- [Headers](#headers)
- [Response Shapes](#response-shapes)
- [Health Checks](#health-checks)
- [Endpoint Contracts](#endpoint-contracts)
- [Pagination](#pagination)
- [Error Codes](#error-codes)

## API Standards

- Base path: `/api`.
- Format: JSON request and response bodies.
- Authentication: Bearer JWT for protected endpoints.
- Authorization: role and scope checks on protected actions.
- Validation: validate params, query, and body before service execution.
- Audit: write audit logs for sensitive lifecycle changes.

## Headers

| Header | Required | Purpose |
| --- | --- | --- |
| `Content-Type: application/json` | For JSON bodies | Declares request body format. |
| `Authorization: Bearer <token>` | Protected endpoints | Authenticates current user. |

## Response Shapes

Success:

```json
{
  "success": true,
  "data": {},
  "message": "Operation completed successfully."
}
```

Error:

```json
{
  "success": false,
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "Invalid request.",
    "details": {}
  }
}
```

## Health Checks

Health routes are intentionally public and are not mounted under `/api`.

| Endpoint | Purpose | Headers | Authorization | Example Response | HTTP Codes | Related Tables |
| --- | --- | --- | --- | --- | --- | --- |
| `GET /health` | Confirm the backend process is running. | None | Public | `{"service":"assetflow-backend","status":"ok"}` | `200` | None |
| `GET /health/db` | Confirm the backend can query PostgreSQL. | None | Public | `{"service":"assetflow-backend","database":"connected"}` | `200`, `500` | PostgreSQL connection |

## Endpoint Contracts

### Authentication

| Endpoint | Purpose | Headers | Authorization | Example Request | Example Response | Validation | Possible Errors | HTTP Codes | Related Tables |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `POST /auth/login` | Authenticate user and return JWT. | `Content-Type` | Public | `{"email":"admin@assetflow.local","password":"secret"}` | `{"token":"jwt","user":{"id":"usr_1","role":"ADMIN"}}` | Email and password required. | Invalid credentials, inactive user. | `200`, `400`, `401` | `users` |
| `GET /auth/me` | Return current user context. | `Authorization` | Any authenticated user. | None | `{"id":"usr_1","email":"admin@assetflow.local","role":"ADMIN"}` | Valid JWT required. | Missing token, invalid token. | `200`, `401` | `users`, `departments` |
| `POST /auth/logout` | Support token-clearing client workflow. | `Authorization` | Any authenticated user. | None | `{"message":"Logged out"}` | Valid JWT required. | Missing token. | `200`, `401` | None |

### Dashboard

| Endpoint | Purpose | Headers | Authorization | Example Request | Example Response | Validation | Possible Errors | HTTP Codes | Related Tables |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `GET /dashboard/overview` | Return a single optimized dashboard payload with overview counts, alerts, quick actions, recent lifecycle activity, notification badge data, and settings summary. | `Authorization` | Any authenticated user, scoped by role. | `?departmentId=dept_1` | `{"overview":{"availableAssets":3,"allocatedAssets":1,"unreadNotifications":4,"notificationBadgeCount":4},"alerts":[],"quickActions":[],"recentActivity":[],"latestActivity":[],"notifications":{"recent":[],"criticalAlerts":[]},"settings":{"company":{"companyName":"AssetFlow"},"profile":{"theme":"SYSTEM"}}}` | Optional `departmentId` must be a UUID. Managers can only request their own department; employees cannot use department filtering. | Missing token, invalid department scope. | `200`, `400`, `401`, `403` | `assets`, `allocations`, `transfers`, `bookings`, `maintenance_tickets`, `audit_records`, `notifications`, `system_settings`, `user_preferences` |

Dashboard response fields:

| Field | Description |
| --- | --- |
| `overview.totalAssets` | Count of visible assets for the current user's dashboard scope. |
| `overview.availableAssets` | Count of visible assets with `AVAILABLE` status. |
| `overview.allocatedAssets` | Count of visible assets with `ALLOCATED` status. |
| `overview.maintenanceAssets` | Count of visible assets with `MAINTENANCE` status. |
| `overview.activeBookings` | Count of approved bookings currently in progress. |
| `overview.pendingBookings` | Count of pending booking approvals in scope. |
| `overview.upcomingBookings` | Count of approved upcoming bookings in scope. |
| `overview.pendingTransfers` | Count of pending transfer approvals in scope. |
| `overview.upcomingReturns` | Count of approved bookings with a future return/end time. |
| `overview.pendingMaintenance` | Count of requested or legacy open maintenance tickets in scope. |
| `overview.approvedMaintenance` | Count of approved maintenance tickets waiting for technician assignment. |
| `overview.assignedMaintenance` | Count of assigned maintenance tickets not yet started. |
| `overview.inProgressMaintenance` | Count of active in-progress maintenance tickets. |
| `overview.resolvedTodayMaintenance` | Count of resolved maintenance tickets with `resolvedAt` today. |
| `overview.upcomingMaintenance` | Count of approved or assigned maintenance tickets that are ready for work. |
| `overview.activeAudits` | Count of active or in-progress audits in scope. |
| `overview.auditsCompleted` | Count of completed or closed audits in scope. |
| `overview.assetsVerified` | Count of audit-scope assets that have been verified. |
| `overview.pendingVerification` | Count of assigned audit assets still waiting for verification. |
| `overview.discrepanciesFound` | Count of audit discrepancies found in scope. |
| `alerts` | Status-based alerts derived from overdue approved bookings, pending approvals, active maintenance, and audit warning results. |
| `quickActions` | Role-based action descriptors for the frontend to map to UI actions. |
| `recentActivity` | Combined latest allocations, transfers, bookings, maintenance tickets, and audits ordered newest first. |
| `settings.company` | Company name, logo URL, timezone, currency, and language for dashboard chrome. |
| `settings.profile` | Current user's display name, avatar URL, theme, timezone, language, and notification preferences. |

### Organization Setup

| Endpoint | Purpose | Headers | Authorization | Example Request | Example Response | Validation | Possible Errors | HTTP Codes | Related Tables |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `GET /organization/overview` | Return lightweight summary counts for the Organization Setup screen. | `Authorization` | Admin full view; Manager scoped read-only; Auditor read-only. | None | `{"departments":{"total":3},"categories":{"total":5},"employees":{"total":4}}` | Valid JWT and permitted role. | Missing token, forbidden role. | `200`, `401`, `403` | `departments`, `categories`, `users` |
| `GET /users` | List employees/users with filters and safe profile data. | `Authorization` | Admin, Manager scoped, Auditor scoped. | `?departmentId=dept_1&role=EMPLOYEE&search=Asha&page=1&limit=20` | `{"items":[{"id":"usr_2","name":"Asha","department":{"manager":{"id":"usr_3"}}}],"total":1}` | Filter IDs and enum values must be valid. Password hashes are never returned. | Forbidden, invalid filter. | `200`, `400`, `401`, `403` | `users`, `departments` |
| `GET /users/:id` | Fetch safe employee/user profile details for edit and profile views. | `Authorization` | Admin and Auditor can read all; Manager can read same-department users only. | None | `{"id":"usr_2","name":"Asha","email":"asha@company.com","role":"EMPLOYEE","department":{"id":"dept_1","name":"IT"}}` | Valid user UUID. Password hashes are never returned. | Not found, forbidden manager scope, invalid UUID. | `200`, `400`, `401`, `403`, `404` | `users`, `departments` |
| `POST /users` | Create employee/user. | `Content-Type`, `Authorization` | Admin. | `{"name":"Asha","email":"asha@company.com","password":"password123","role":"EMPLOYEE","departmentId":"dept_1"}` | `{"id":"usr_2","status":"ACTIVE"}` | Unique email, valid role, valid department. | Duplicate email, invalid role, missing department. | `201`, `400`, `401`, `403`, `404`, `409` | `users`, `departments`, `audit_logs` |
| `PATCH /users/:id` | Update employee/user profile, department, role, or status. | `Content-Type`, `Authorization` | Admin. | `{"departmentId":"dept_2","status":"ACTIVE"}` | `{"id":"usr_2","departmentId":"dept_2","status":"ACTIVE"}` | Valid user ID, valid department, and allowed fields. | Not found, forbidden. | `200`, `400`, `401`, `403`, `404` | `users`, `departments`, `audit_logs` |
| `DELETE /users/:id` | Deactivate employee/user by setting `status=INACTIVE`. | `Authorization` | Admin. | None | `{"id":"usr_2","status":"INACTIVE"}` | Valid user UUID. Admins cannot deactivate their own account. | Not found, forbidden, self-deactivation conflict. | `200`, `400`, `401`, `403`, `404`, `409` | `users`, `audit_logs` |
| `GET /departments` | List departments with parent/head metadata and employee counts. | `Authorization` | Admin, Manager read-only, Auditor read-only. | `?status=ACTIVE&parentDepartmentId=dept_1&search=IT&page=1&limit=20` | `{"items":[{"id":"dept_1","name":"IT","departmentHead":{"id":"usr_3"},"employeeCount":8}],"total":1}` | Status, UUID filters, and pagination must be valid. | Invalid filter, forbidden role. | `200`, `400`, `401`, `403` | `departments`, `users`, `assets` |
| `POST /departments` | Create department. | `Content-Type`, `Authorization` | Admin. | `{"name":"Finance","code":"FIN","managerId":"usr_3","parentDepartmentId":"dept_1"}` | `{"id":"dept_2","code":"FIN","employeeCount":0}` | Unique name/code, valid active department head, valid parent department, no circular parent relationship. | Duplicate code/name, invalid head, missing parent. | `201`, `400`, `401`, `403`, `404`, `409` | `departments`, `users`, `audit_logs` |
| `PATCH /departments/:id` | Update department metadata, status, parent, or department head. | `Content-Type`, `Authorization` | Admin. | `{"managerId":"usr_3","parentDepartmentId":null}` | `{"id":"dept_2","managerId":"usr_3","parentDepartmentId":null}` | Valid department, valid active department head, and no circular parent assignment. | Not found, invalid manager, circular parent. | `200`, `400`, `401`, `403`, `404`, `409` | `departments`, `users`, `audit_logs` |
| `DELETE /departments/:id` | Soft-delete department by setting `status=INACTIVE`. | `Authorization` | Admin. | None | `{"id":"dept_2","status":"INACTIVE"}` | Valid department ID. | Not found, forbidden. | `200`, `400`, `401`, `403`, `404` | `departments`, `audit_logs` |
| `GET /categories` | List asset categories with pagination, status, description, search, and asset counts. | `Authorization` | Admin, Manager read-only, Auditor read-only. | `?status=ACTIVE&search=Laptop&page=1&limit=20` | `{"items":[{"id":"cat_1","name":"Laptop","description":"Portable computers","assetCount":2}],"total":1}` | Status/search/pagination filters must be valid. | Invalid filter, forbidden role. | `200`, `400`, `401`, `403` | `categories`, `assets` |
| `POST /categories` | Create asset category. | `Content-Type`, `Authorization` | Admin. | `{"name":"Laptop","code":"LAP"}` | `{"id":"cat_1","code":"LAP"}` | Unique name and code. | Duplicate code. | `201`, `400`, `401`, `403`, `409` | `categories`, `audit_logs` |
| `PATCH /categories/:id` | Update asset category. | `Content-Type`, `Authorization` | Admin. | `{"description":"Computing assets"}` | `{"id":"cat_1","description":"Computing assets"}` | Valid category ID and allowed fields. | Not found, duplicate code. | `200`, `400`, `401`, `403`, `404`, `409` | `categories`, `audit_logs` |
| `DELETE /categories/:id` | Soft-delete category by setting `status=INACTIVE`. | `Authorization` | Admin. | None | `{"id":"cat_1","status":"INACTIVE"}` | Valid category ID. | Not found, forbidden. | `200`, `400`, `401`, `403`, `404` | `categories`, `audit_logs` |

Organization Setup notes:

- `departmentHead` is the department `manager` relation returned as a frontend-friendly alias.
- `employeeCount`, `assetCount`, and `childDepartmentCount` are returned on department list and write responses.
- Employee manager relationship is represented through `department.manager`; there is no direct employee manager field.
- Delete endpoints are soft deletes and do not remove records from historical workflows.

### Assets and Allocations

| Endpoint | Purpose | Headers | Authorization | Example Request | Example Response | Validation | Possible Errors | HTTP Codes | Related Tables |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `GET /assets` | List assets with frontend filters, pagination, sorting, and useful relations. | `Authorization` | Admin and Auditor read all; Manager is department-scoped; Employee can read own/bookable assets. | `?search=laptop&categoryId=uuid&departmentId=uuid&status=AVAILABLE&serialNumber=SN-LAP-001&assetTag=LAP-001&qrCode=assetflow%3Aasset%3A...&location=Store&page=1&limit=20&sortBy=name&sortOrder=asc` | `{"items":[{"id":"ast_1","assetCode":"LAP-001","assetTag":"LAP-001","serialNumber":"SN-LAP-001","category":{},"department":{},"currentAllocation":null,"maintenanceStatus":null,"createdBy":{},"updatedBy":{}}],"total":1}` | Valid UUID filters, enum status, supported sort fields: `createdAt`, `updatedAt`, `name`, `assetCode`, `serialNumber`, `status`, `location`, `purchaseDate`. | Missing token, invalid filter, forbidden department scope. | `200`, `400`, `401`, `403` | `assets`, `categories`, `departments`, `allocations`, `maintenance_tickets`, `users` |
| `GET /assets/lookup` | Single asset lookup for frontend search box by general query, asset tag/code, serial number, or QR code. | `Authorization` | Same read scope as asset list. | `?q=LAP-001`, `?assetTag=LAP-001`, `?serialNumber=SN-LAP-001`, `?qrCode=assetflow%3Aasset%3Auuid%3ALAP-001` | `{"id":"ast_1","assetCode":"LAP-001","assetTag":"LAP-001","qrCode":"assetflow:asset:uuid:LAP-001"}` | At least one lookup parameter is required. | Missing query, not found, forbidden scope. | `200`, `400`, `401`, `403`, `404` | `assets`, `categories`, `departments`, `allocations`, `maintenance_tickets` |
| `POST /assets` | Register a new asset. | `Content-Type`, `Authorization` | Admin full; Manager only in assigned department. | `{"assetTag":"LAP-003","serialNumber":"SN-LAP-003","name":"Laptop","categoryId":"uuid","departmentId":"uuid","condition":"GOOD","location":"IT Store","purchaseValue":95000,"purchaseDate":"2026-07-01T00:00:00.000Z","warrantyExpiry":"2027-07-01T00:00:00.000Z","isBookable":true}` | `{"id":"ast_1","assetCode":"LAP-003","assetTag":"LAP-003","status":"AVAILABLE","qrCode":"assetflow:asset:uuid:LAP-003"}` | `assetTag` or `assetCode` required; asset tag and serial number must be unique; category and department must exist and be active. | Duplicate tag, duplicate serial number, invalid/inactive category, invalid/inactive department, forbidden scope. | `201`, `400`, `401`, `403`, `404`, `409` | `assets`, `categories`, `departments`, `users`, `audit_logs` |
| `GET /assets/:id` | Get asset detail and lifecycle summary. | `Authorization` | Same read scope as asset list. | None | `{"id":"ast_1","assetCode":"LAP-001","assetTag":"LAP-001","status":"ALLOCATED","category":{},"department":{},"currentAllocation":{},"maintenanceStatus":null,"transfers":[],"bookings":[],"maintenanceTickets":[],"auditRecords":[]}` | Valid asset UUID. | Not found, forbidden scope. | `200`, `400`, `401`, `403`, `404` | `assets`, `allocations`, `transfers`, `bookings`, `maintenance_tickets`, `audit_records` |
| `PATCH /assets/:id` | Update asset metadata and editable identifiers. | `Content-Type`, `Authorization` | Admin full; Manager only assigned department assets. | `{"assetTag":"LAP-003A","serialNumber":"SN-LAP-003A","location":"Mumbai Office","condition":"GOOD"}` | `{"id":"ast_1","assetTag":"LAP-003A","serialNumber":"SN-LAP-003A","location":"Mumbai Office"}` | Valid asset UUID; unique tag/serial; active category/department when changed; supported asset status enum. | Not found, forbidden scope, duplicate identifiers, invalid status transition. | `200`, `400`, `401`, `403`, `404`, `409` | `assets`, `categories`, `departments`, `audit_logs` |
| `DELETE /assets/:id` | Soft-delete an asset by marking it `RETIRED`. | `Authorization` | Admin full; Manager only assigned department assets. | None | `{"id":"ast_1","status":"RETIRED"}` | Valid asset UUID; asset must not have an active allocation. | Not found, forbidden scope, active allocation conflict. | `200`, `400`, `401`, `403`, `404`, `409` | `assets`, `allocations`, `audit_logs` |
| `POST /assets/:id/allocate` | Allocate asset to user or department. | `Content-Type`, `Authorization` | Admin, Manager scoped. | `{"userId":"usr_2","notes":"Initial assignment"}` | `{"allocationId":"alc_1","assetStatus":"ALLOCATED"}` | Asset available, valid assignee, one active allocation. | Conflict, invalid assignee. | `201`, `400`, `401`, `403`, `404`, `409` | `assets`, `allocations`, `users`, `departments`, `audit_logs` |
| `POST /assets/:id/retire` | Retire asset with reason. | `Content-Type`, `Authorization` | Admin. | `{"reason":"End of life"}` | `{"id":"ast_1","status":"RETIRED"}` | No blocking active workflow unless admin override defined. | Active allocation, not found. | `200`, `400`, `401`, `403`, `404`, `409` | `assets`, `allocations`, `audit_logs` |
| `GET /assets/:id/qr` | Generate QR identity data for the asset. | `Authorization` | Admin, Manager, Auditor scoped. | None | `{"assetId":"ast_1","assetCode":"LAP-001","assetTag":"LAP-001","serialNumber":"SN-LAP-001","qrValue":"assetflow:asset:uuid:LAP-001","imageDataUrl":"data:image/png;base64,..."}` | Valid asset UUID and role scope. | Not found, forbidden. | `200`, `400`, `401`, `403`, `404` | `assets` |

Asset responses intentionally expose both `assetCode` and `assetTag`. The database field is `asset_code`; `assetTag` is an API alias for frontend terminology. QR values are generated from the asset identity and are not stored in a separate table.

### Allocations

| Endpoint | Purpose | Headers | Authorization | Example Request | Example Response | Validation | Possible Errors | HTTP Codes | Related Tables |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `GET /allocations` | List allocation history with filters and pagination. | `Authorization` | Admin full; Manager department scoped; Employee own allocations; Auditor read-only. | `?employeeId=uuid&departmentId=uuid&assetId=uuid&status=ACTIVE&from=2026-07-01T00:00:00.000Z&to=2026-07-31T23:59:59.000Z&page=1&limit=20&sortBy=assignedAt&sortOrder=desc` | `{"items":[{"id":"alc_1","status":"ACTIVE","asset":{},"allocatedTo":{},"allocatedBy":{},"department":{},"startDate":"...","returnDate":null,"returnCondition":null,"returnReason":null,"transferSource":null}],"total":1}` | Valid UUID filters, date range, allocation status, pagination, and supported sort fields. | Missing token, invalid filter, forbidden scope. | `200`, `400`, `401`, `403` | `allocations`, `assets`, `users`, `departments`, `transfers` |
| `GET /allocations/:id` | Fetch allocation detail and history metadata. | `Authorization` | Admin full; Manager department scoped; Employee own allocation; Auditor read-only. | None | `{"id":"alc_1","asset":{},"allocatedTo":{},"allocatedBy":{},"department":{},"transferSource":null}` | Valid allocation UUID. | Not found, forbidden scope. | `200`, `400`, `401`, `403`, `404` | `allocations`, `assets`, `users`, `departments`, `transfers` |
| `POST /allocations` | Allocate an available asset to an employee and/or department. | `Content-Type`, `Authorization` | Admin full; Manager assigned department only. | `{"assetId":"uuid","userId":"uuid","departmentId":"uuid","notes":"Initial assignment"}` | `{"allocation":{"id":"alc_1","status":"ACTIVE"},"asset":{"id":"ast_1","status":"ALLOCATED"}}` | Asset must be allocatable; destination user/department must be active; one active allocation per asset is enforced by service logic and a database partial unique index. | Already allocated, retired/lost/maintenance asset, inactive destination, forbidden scope. | `201`, `400`, `401`, `403`, `404`, `409` | `allocations`, `assets`, `users`, `departments`, `audit_logs`, `notifications` |
| `PATCH /allocations/:id` | Update an active allocation's destination metadata or notes. | `Content-Type`, `Authorization` | Admin full; Manager assigned department only. | `{"userId":"uuid","departmentId":"uuid","notes":"Updated assignment"}` | `{"id":"alc_1","userId":"uuid","departmentId":"uuid","notes":"Updated assignment"}` | Allocation must be active; destination user/department must be active; allocation must retain a user or department. | Not found, inactive destination, already returned/transferred, forbidden scope. | `200`, `400`, `401`, `403`, `404`, `409` | `allocations`, `assets`, `users`, `departments`, `audit_logs` |
| `POST /allocations/:id/return` | Return an active allocation and make the asset available unless maintenance is open. | `Content-Type`, `Authorization` | Admin full; Manager department scoped; Employee can return own allocation. | `{"returnCondition":"GOOD","reason":"Project completed","notes":"Returned at IT desk"}` | `{"id":"alc_1","status":"RETURNED","returnedAt":"...","returnCondition":"GOOD","returnReason":"Project completed"}` | Allocation must be active; return reason and condition are required/validated. | Not found, already returned/transferred, forbidden scope. | `200`, `400`, `401`, `403`, `404`, `409` | `allocations`, `assets`, `audit_logs`, `notifications`, `maintenance_tickets` |

### Transfers

| Endpoint | Purpose | Headers | Authorization | Example Request | Example Response | Validation | Possible Errors | HTTP Codes | Related Tables |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `GET /transfers` | List transfer records with filters and pagination. | `Authorization` | Admin full; Manager department scoped; Employee related transfers; Auditor read-only. | `?status=PENDING&assetId=uuid&employeeId=uuid&departmentId=uuid&from=2026-07-01T00:00:00.000Z&to=2026-07-31T23:59:59.000Z&page=1&limit=20&sortBy=requestedAt&sortOrder=desc` | `{"items":[{"id":"trn_1","status":"PENDING","asset":{},"requestedBy":{},"fromUser":{},"toUser":{}}],"total":1}` | Valid status, UUID filters, date range, pagination, and supported sort fields. | Missing token, invalid filter, forbidden scope. | `200`, `400`, `401`, `403` | `transfers`, `assets`, `users`, `departments`, `allocations` |
| `POST /transfers` | Create transfer request for an actively allocated asset. | `Content-Type`, `Authorization` | Admin, Manager scoped, Employee own assigned asset. | `{"assetId":"ast_1","toUserId":"usr_4","reason":"Role change"}` | `{"id":"trn_1","status":"PENDING"}` | Asset must have exactly one active allocation; no other pending transfer may exist for the asset; destination user/department must be active and differ from current assignment. | Invalid destination, no active allocation, duplicate pending transfer, forbidden scope. | `201`, `400`, `401`, `403`, `404`, `409` | `transfers`, `assets`, `allocations`, `users`, `departments`, `audit_logs` |
| `GET /transfers/:id` | Fetch transfer detail. | `Authorization` | Admin full; Manager department scoped; Employee related transfer; Auditor read-only. | None | `{"id":"trn_1","status":"PENDING","asset":{"id":"ast_1"},"createdAllocation":null}` | Valid transfer UUID. | Not found, forbidden scope. | `200`, `400`, `401`, `403`, `404` | `transfers`, `assets`, `users`, `departments`, `allocations` |
| `PATCH /transfers/:id/approve` | Approve transfer, close old allocation, create new allocation, update asset responsibility, record activity, and notify users. | `Content-Type`, `Authorization` | Admin full; Manager source department scoped. | `{"decisionNotes":"Approved"}` | `{"id":"trn_1","status":"APPROVED","createdAllocation":{"id":"alc_2","status":"ACTIVE"}}` | Transfer must be pending and source allocation must still be active. | Already approved/rejected/cancelled, no active source allocation, forbidden scope. | `200`, `400`, `401`, `403`, `404`, `409` | `transfers`, `allocations`, `assets`, `audit_logs`, `notifications` |
| `PATCH /transfers/:id/reject` | Reject pending transfer. | `Content-Type`, `Authorization` | Admin full; Manager source department scoped. | `{"decisionNotes":"Asset still required"}` | `{"id":"trn_1","status":"REJECTED"}` | Transfer must be pending. | Already decided, forbidden scope. | `200`, `400`, `401`, `403`, `404`, `409` | `transfers`, `audit_logs`, `notifications` |
| `PATCH /transfers/:id/cancel` | Cancel pending transfer. | `Content-Type`, `Authorization` | Requester or Admin. | `{"reason":"No longer needed"}` | `{"id":"trn_1","status":"CANCELLED"}` | Transfer must be pending. | Forbidden, already decided. | `200`, `400`, `401`, `403`, `404` | `transfers`, `audit_logs`, `notifications` |

Transfer approval is atomic. The backend closes the old active allocation with status `TRANSFERRED`, creates exactly one new active allocation linked by `sourceTransferId`, updates the asset department/status, records an audit log, and creates notifications where supported.

### Bookings

| Endpoint | Purpose | Headers | Authorization | Example Request | Example Response | Validation | Possible Errors | HTTP Codes | Related Tables |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `GET /bookings` | List resource bookings with filters and pagination. | `Authorization` | Admin all; Manager department scoped; Employee own bookings; Auditor read-only. | `?resourceId=uuid&status=REQUESTED&departmentId=uuid&from=2026-07-12T00:00:00.000Z&to=2026-07-13T00:00:00.000Z&page=1&limit=20&sortBy=startTime&sortOrder=asc` | `{"items":[{"id":"bkg_1","resourceId":"ast_2","status":"REQUESTED","lifecycleStatus":"REQUESTED","resource":{},"bookedBy":{},"department":{}}],"total":1}` | Valid UUID filters, booking status, date range, pagination, and sort fields. | Missing token, invalid filters, forbidden scope. | `200`, `400`, `401`, `403` | `bookings`, `assets`, `users`, `departments` |
| `GET /bookings/calendar` | Return frontend-ready calendar events. | `Authorization` | Same read scope as booking list. | `?resourceId=uuid&date=2026-07-12`, `?week=2026-W29`, `?month=2026-07` | `[{"id":"bkg_1","resourceId":"ast_2","title":"Demo","start":"2026-07-12T09:00:00.000Z","end":"2026-07-12T10:00:00.000Z","status":"APPROVED","bookedBy":{},"department":{}}]` | `date`, `week`, or `month` is required; optional resource must be a bookable asset. | Invalid date selector, missing/invalid resource, forbidden scope. | `200`, `400`, `401`, `403`, `404`, `409` | `bookings`, `assets`, `users`, `departments` |
| `GET /bookings/availability` | Return available slots, occupied slots, conflicts, and next available slot for one resource/date. | `Authorization` | Authenticated users scoped by role. | `?resourceId=uuid&date=2026-07-12` | `{"resourceId":"uuid","date":"2026-07-12","availableSlots":[],"occupiedSlots":[],"conflicts":[],"nextAvailableSlot":{"start":"...","end":"..."}}` | `resourceId` and `date` are required; resource must be bookable. | Invalid resource/date, forbidden scope. | `200`, `400`, `401`, `403`, `404`, `409` | `bookings`, `assets`, `users` |
| `GET /bookings/:id` | Fetch booking detail. | `Authorization` | Admin all; Manager department scoped; Employee own booking; Auditor read-only. | None | `{"id":"bkg_1","resourceId":"ast_2","status":"APPROVED","lifecycleStatus":"APPROVED","resource":{},"bookedBy":{},"approvedBy":{}}` | Valid booking UUID. | Not found, forbidden scope. | `200`, `400`, `401`, `403`, `404` | `bookings`, `assets`, `users`, `departments` |
| `POST /bookings` | Create booking request. | `Content-Type`, `Authorization` | Admin, Manager, Employee. Auditor read-only. | `{"resourceId":"uuid","startTime":"2026-07-15T09:00:00.000Z","endTime":"2026-07-15T11:00:00.000Z","purpose":"Demo"}` | `{"id":"bkg_1","resourceId":"uuid","status":"REQUESTED","lifecycleStatus":"REQUESTED"}` | Resource must be bookable and not maintenance/retired/lost; time must be future; no duplicate or overlapping booking may exist. | Conflict, invalid time range, invalid resource, forbidden scope. | `201`, `400`, `401`, `403`, `404`, `409` | `bookings`, `assets`, `audit_logs`, `notifications` |
| `PATCH /bookings/:id` | Update requested booking metadata, resource, or time. Can mark eligible ended approved/active bookings completed. | `Content-Type`, `Authorization` | Requester, Admin, or Manager scoped for edits; Admin/Manager for completion. | `{"purpose":"Updated demo","startTime":"2026-07-15T10:00:00.000Z","endTime":"2026-07-15T11:00:00.000Z"}` | `{"id":"bkg_1","purpose":"Updated demo"}` | Booking must be future and requested for normal edits; new slot must be conflict-free. | Past booking, invalid transition, conflict, forbidden scope. | `200`, `400`, `401`, `403`, `404`, `409` | `bookings`, `assets`, `audit_logs` |
| `DELETE /bookings/:id` | Cancel a future booking using REST delete semantics. | `Authorization` | Requester, Admin, Manager scoped. | None | `{"id":"bkg_1","status":"CANCELLED"}` | Booking must be future and cancellable. | Already terminal, past booking, forbidden scope. | `200`, `400`, `401`, `403`, `404`, `409` | `bookings`, `audit_logs`, `notifications` |
| `PATCH /bookings/:id/approve` | Approve requested booking after conflict check. | `Content-Type`, `Authorization` | Admin all; Manager department scoped. | `{"decisionNotes":"Available"}` | `{"id":"bkg_1","status":"APPROVED"}` | Booking must be `REQUESTED` or legacy `PENDING`; no approved/active overlap may exist. | Conflict, already decided, past booking, forbidden scope. | `200`, `400`, `401`, `403`, `404`, `409` | `bookings`, `audit_logs`, `notifications` |
| `PATCH /bookings/:id/reject` | Reject requested booking. | `Content-Type`, `Authorization` | Admin all; Manager department scoped. | `{"decisionNotes":"Unavailable"}` | `{"id":"bkg_1","status":"REJECTED"}` | Booking must be `REQUESTED` or legacy `PENDING` and future. | Already decided, past booking, forbidden scope. | `200`, `400`, `401`, `403`, `404`, `409` | `bookings`, `audit_logs`, `notifications` |
| `PATCH /bookings/:id/cancel` | Cancel booking. | `Content-Type`, `Authorization` | Requester, Admin, Manager scoped. | `{"reason":"No longer needed"}` | `{"id":"bkg_1","status":"CANCELLED"}` | Booking must be future and cancellable. | Forbidden, invalid status, past booking. | `200`, `400`, `401`, `403`, `404`, `409` | `bookings`, `audit_logs`, `notifications` |

Booking lifecycle uses `REQUESTED -> APPROVED -> ACTIVE -> COMPLETED`, with `REJECTED` and `CANCELLED` as terminal alternatives. The backend also accepts legacy `PENDING` as an alias for requested bookings. Cancelled and rejected bookings are excluded from conflict checks and free the slot. `ACTIVE` and `COMPLETED` states are synchronized opportunistically when booking APIs are used.

### Maintenance

| Endpoint | Purpose | Headers | Authorization | Example Request | Example Response | Validation | Possible Errors | HTTP Codes | Related Tables |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `GET /maintenance` | List maintenance tickets with search, filters, pagination, and role scope. | `Authorization` | Admin all; Manager department scoped; Employee own or assigned tickets; Auditor read-only. | `?status=REQUESTED&priority=HIGH&assetId=uuid&departmentId=uuid&page=1&limit=20` | `{"items":[{"id":"mnt_1","status":"REQUESTED","lifecycleStatus":"REQUESTED","asset":{},"reportedBy":{},"assignedTechnician":null}],"total":1}` | Valid UUID filters, maintenance status, priority, date range, and pagination. | Missing token, invalid filter, forbidden scope. | `200`, `400`, `401`, `403` | `maintenance_tickets`, `assets`, `users`, `departments` |
| `POST /maintenance` | Create maintenance request and move the asset to `MAINTENANCE`. | `Content-Type`, `Authorization` | Admin all; Manager department scoped; Employee can create own ticket. Auditor read-only. | `{"assetId":"uuid","priority":"HIGH","issueSummary":"Battery failure","issueDescription":"Battery drains quickly"}` | `{"id":"mnt_1","status":"REQUESTED","assignedTechnicianId":null}` | Asset must exist, not be `RETIRED` or `LOST`, and must not already have an active maintenance ticket. Duplicate pending requests are rejected. | Asset unavailable, duplicate request, forbidden scope. | `201`, `400`, `401`, `403`, `404`, `409` | `maintenance_tickets`, `assets`, `audit_logs`, `notifications` |
| `GET /maintenance/:id` | Fetch maintenance ticket detail. | `Authorization` | Admin all; Manager department scoped; Employee own or assigned ticket; Auditor read-only. | None | `{"id":"mnt_1","status":"ASSIGNED","asset":{},"reportedBy":{},"assignedTechnician":{}}` | Valid maintenance ticket UUID. | Not found, forbidden scope. | `200`, `400`, `401`, `403`, `404` | `maintenance_tickets`, `assets`, `users` |
| `PATCH /maintenance/:id` | Update editable ticket metadata. Legacy status updates are accepted only when they can be translated into valid workflow actions. | `Content-Type`, `Authorization` | Admin all; Manager department scoped. | `{"priority":"CRITICAL","issueSummary":"Battery swollen"}` | `{"id":"mnt_1","priority":"CRITICAL"}` | Ticket must not be closed/rejected; technician IDs must reference active non-auditor users. | Invalid technician, closed ticket, forbidden scope. | `200`, `400`, `401`, `403`, `404`, `409` | `maintenance_tickets`, `users`, `audit_logs` |
| `DELETE /maintenance/:id` | Delete requested or rejected maintenance tickets and restore asset state when no active maintenance remains. | `Authorization` | Admin all; Manager department scoped. | None | `{"id":"mnt_1","status":"REQUESTED"}` | Ticket must be requested/open/rejected. | Already active/resolved/closed, forbidden scope. | `200`, `400`, `401`, `403`, `404`, `409` | `maintenance_tickets`, `assets`, `audit_logs` |
| `PATCH /maintenance/:id/approve` | Approve requested maintenance. | `Content-Type`, `Authorization` | Admin all; Manager department scoped. | `{"decisionNotes":"Approved for repair"}` | `{"id":"mnt_1","status":"APPROVED"}` | Ticket must be `REQUESTED` or legacy `OPEN`. | Already approved, invalid transition, forbidden scope. | `200`, `400`, `401`, `403`, `404`, `409` | `maintenance_tickets`, `audit_logs`, `notifications` |
| `PATCH /maintenance/:id/reject` | Reject requested maintenance and restore asset state. | `Content-Type`, `Authorization` | Admin all; Manager department scoped. | `{"decisionNotes":"Duplicate"}` | `{"id":"mnt_1","status":"REJECTED"}` | Ticket must be `REQUESTED` or legacy `OPEN`. | Already decided, forbidden scope. | `200`, `400`, `401`, `403`, `404`, `409` | `maintenance_tickets`, `assets`, `audit_logs`, `notifications` |
| `PATCH /maintenance/:id/assign` | Assign or reassign a technician. | `Content-Type`, `Authorization` | Admin all; Manager department scoped. | `{"assignedTechnicianId":"uuid"}` | `{"id":"mnt_1","status":"ASSIGNED","assignedTechnicianId":"uuid","assignedAt":"..."}` | Ticket must be approved or already assigned; technician must be active and not an auditor. | Invalid technician, invalid transition. | `200`, `400`, `401`, `403`, `404`, `409` | `maintenance_tickets`, `users`, `audit_logs`, `notifications` |
| `PATCH /maintenance/:id/start` | Start assigned maintenance. | `Content-Type`, `Authorization` | Admin all; Manager department scoped. | `{}` | `{"id":"mnt_1","status":"IN_PROGRESS","startedAt":"..."}` | Ticket must be assigned and have a technician. | Missing technician, invalid transition. | `200`, `400`, `401`, `403`, `404`, `409` | `maintenance_tickets`, `audit_logs`, `notifications` |
| `PATCH /maintenance/:id/resolve` | Mark in-progress maintenance as resolved. | `Content-Type`, `Authorization` | Admin all; Manager department scoped. | `{"resolutionNotes":"Battery replaced","resolutionCost":2500}` | `{"id":"mnt_1","status":"RESOLVED","resolvedAt":"..."}` | Ticket must be in progress; notes are required; cost must be non-negative. | Invalid transition, invalid cost. | `200`, `400`, `401`, `403`, `404`, `409` | `maintenance_tickets`, `audit_logs`, `notifications` |
| `PATCH /maintenance/:id/close` | Close resolved maintenance and restore asset status to `AVAILABLE` or `ALLOCATED` based on active allocation state. | `Content-Type`, `Authorization` | Admin all; Manager department scoped. | `{"resolutionNotes":"Verified after repair"}` | `{"id":"mnt_1","status":"CLOSED","closedAt":"..."}` | Ticket should be resolved. For backward compatibility, an in-progress ticket with resolution notes can be resolved and closed in one call. | Missing resolution, invalid status. | `200`, `400`, `401`, `403`, `404`, `409` | `maintenance_tickets`, `assets`, `audit_logs`, `notifications` |

Maintenance lifecycle uses `REQUESTED -> APPROVED -> ASSIGNED -> IN_PROGRESS -> RESOLVED -> CLOSED`, with `REJECTED` as the terminal alternative. Legacy `OPEN` tickets are treated as requested tickets. Asset status uses the existing `MAINTENANCE` enum value to represent the under-maintenance state.

### Audit, Reports, and Notifications

| Endpoint | Purpose | Headers | Authorization | Example Request | Example Response | Validation | Possible Errors | HTTP Codes | Related Tables |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `GET /audits` | List audit cycles with role scope, filters, pagination, and completion counters. | `Authorization` | Admin and Auditor all; Manager department scoped; Employee assigned audit assets only. | `?status=ACTIVE&departmentId=uuid&result=MISSING&page=1&limit=20` | `{"items":[{"id":"aud_1","title":"Q3 IT Audit","status":"ACTIVE","totalAssets":10,"verifiedAssets":3,"pendingVerification":7}],"total":1}` | Valid audit status, result, UUID filters, date range, and pagination. | Missing token, invalid filter, forbidden scope. | `200`, `400`, `401`, `403` | `audits`, `audit_records`, `assets`, `departments`, `users` |
| `POST /audits` | Create an audit cycle and snapshot assigned asset scope. Legacy `{"assetId","result"}` payloads still create one-off audit records for backward compatibility. | `Content-Type`, `Authorization` | Admin and Auditor full; Manager department scoped. | `{"title":"Q3 IT Audit","departmentId":"uuid","assetIds":["uuid"],"assignedAuditorId":"uuid"}` | `{"id":"aud_1","status":"PLANNED","totalAssets":3,"records":[...]}` | Audit must include at least one asset; managers can only scope their department; assigned auditor must be active. | Empty scope, invalid asset, invalid auditor, forbidden scope. | `201`, `400`, `401`, `403`, `404`, `409` | `audits`, `audit_records`, `assets`, `audit_logs`, `notifications` |
| `GET /audits/:id` | Fetch audit cycle detail, assigned assets, verification status, and discrepancies. | `Authorization` | Admin/Auditor all; Manager department scoped; Employee assigned audit assets. | None | `{"id":"aud_1","status":"IN_PROGRESS","records":[{"asset":{},"result":"VERIFIED"}],"discrepancies":[]}` | Valid audit UUID. | Not found, forbidden scope. | `200`, `400`, `401`, `403`, `404` | `audits`, `audit_records`, `audit_discrepancies` |
| `PATCH /audits/:id` | Update planned audit metadata, assigned auditor, or asset scope. | `Content-Type`, `Authorization` | Admin/Auditor all; Manager department scoped. | `{"title":"Updated audit","assetIds":["uuid"]}` | `{"id":"aud_1","title":"Updated audit","totalAssets":4}` | Audit must still be `PLANNED`; asset scope must be valid. | Closed/active audit, invalid asset, forbidden scope. | `200`, `400`, `401`, `403`, `404`, `409` | `audits`, `audit_records`, `audit_logs` |
| `DELETE /audits/:id` | Delete a planned audit and its unverified scope records. | `Authorization` | Admin/Auditor all; Manager department scoped. | None | `{"id":"aud_1","status":"PLANNED"}` | Audit must be `PLANNED`. | Active/completed/closed audit, forbidden scope. | `200`, `400`, `401`, `403`, `404`, `409` | `audits`, `audit_records`, `audit_logs` |
| `POST /audits/:id/start` | Start a planned audit. | `Authorization` | Admin/Auditor all; Manager department scoped. | None | `{"id":"aud_1","status":"ACTIVE","startedAt":"..."}` | Audit must be `PLANNED`. | Already active/completed/closed, forbidden scope. | `200`, `400`, `401`, `403`, `404`, `409` | `audits`, `audit_logs`, `notifications` |
| `POST /audits/:id/verify` | Verify one scoped asset in an active audit. | `Content-Type`, `Authorization` | Admin/Auditor all; Manager department scoped; Employee assigned asset only. | `{"assetId":"uuid","result":"DAMAGED","locationVerified":"IT Lab","conditionVerified":"DAMAGED","remarks":"Screen cracked"}` | `{"id":"rec_1","assetId":"uuid","result":"DAMAGED","verifiedBy":{},"verifiedAt":"..."}` | Audit must be active/in-progress; asset must be in audit scope; asset cannot be verified twice in same audit. | Duplicate verification, outside scope, invalid transition. | `200`, `400`, `401`, `403`, `404`, `409` | `audit_records`, `audit_discrepancies`, `assets`, `audit_logs`, `notifications` |
| `POST /audits/:id/complete` | Complete an audit after all assigned assets are reviewed. | `Authorization` | Admin/Auditor all; Manager department scoped. | None | `{"id":"aud_1","status":"COMPLETED","completedAt":"..."}` | Every audit record must have a verification result. | Pending verification, invalid status. | `200`, `400`, `401`, `403`, `404`, `409` | `audits`, `audit_records`, `audit_logs`, `notifications` |
| `POST /audits/:id/close` | Close a completed audit. | `Authorization` | Admin/Auditor all; Manager department scoped. | None | `{"id":"aud_1","status":"CLOSED","closedAt":"..."}` | Audit must be `COMPLETED`. | Incomplete audit, invalid status. | `200`, `400`, `401`, `403`, `404`, `409` | `audits`, `audit_logs`, `notifications` |
| `GET /audits/:id/discrepancies` | List discrepancy records generated during verification. | `Authorization` | Same read scope as audit detail. | `?page=1&limit=20` | `{"items":[{"type":"DAMAGED_ASSET","asset":{},"message":"Asset was marked damaged"}],"total":1}` | Valid audit UUID and pagination. | Not found, forbidden scope. | `200`, `400`, `401`, `403`, `404` | `audit_discrepancies`, `audits`, `audit_records`, `assets` |
| `GET /audit-logs` | Read system audit logs. | `Authorization` | Admin, Auditor scoped. | `?entityType=Asset&entityId=ast_1` | `{"items":[{"action":"created","entityId":"ast_1"}]}` | Valid filters and scope. | Forbidden. | `200`, `400`, `401`, `403` | `audit_logs`, `users` |
| `GET /reports/summary` | Return compact backend summary metrics with asset status and condition charts. | `Authorization` | Admin global; Manager department scoped; Employee own data; Auditor read-only. | `?departmentId=uuid` | `{"totalAssets":6,"available":3,"charts":{"byStatus":{"labels":["AVAILABLE"],"datasets":[...]}}}` | Valid filters, date range, and department scope. Managers cannot request another department; employees cannot request department filters. | Forbidden scope, invalid query. | `200`, `400`, `401`, `403` | `assets`, `allocations`, `bookings`, `maintenance_tickets`, `transfers` |
| `GET /reports/dashboard` | Return the frontend analytics dashboard payload in one request. | `Authorization` | Admin global; Manager department scoped; Employee own data; Auditor read-only. | `?from=2026-07-01T00:00:00.000Z&to=2026-07-31T23:59:59.000Z` | `{"summary":{},"charts":{"utilizationByDepartment":{"labels":[],"datasets":[]},"bookingTrend":{}},"idleAssets":[],"frequentlyUsedAssets":[]}` | Valid date range, pagination defaults, optional department scope. | Forbidden scope, invalid date. | `200`, `400`, `401`, `403` | `assets`, `bookings`, `maintenance_tickets`, `audit_records`, `audit_discrepancies` |
| `GET /reports/assets` | Return paginated asset report data with status, condition, and category charts. | `Authorization` | Admin global; Manager department scoped; Employee own asset data; Auditor read-only. | `?status=ALLOCATED&categoryId=uuid&page=1&limit=20` | `{"items":[{"assetCode":"LAP-001","assetTag":"LAP-001"}],"charts":{"status":{"labels":[],"datasets":[]}}}` | Valid status, UUID filters, pagination, and scope. | Forbidden scope, invalid filter. | `200`, `400`, `401`, `403` | `assets`, `categories`, `departments`, `allocations` |
| `GET /reports/bookings` | Return booking utilization, counts, peak hours, status chart, and date trend. | `Authorization` | Admin global; Manager department scoped; Employee own bookings; Auditor read-only. | `?from=2026-07-01T00:00:00.000Z&to=2026-07-31T23:59:59.000Z` | `{"bookingCount":25,"utilizationHours":42,"charts":{"trend":{"labels":[],"datasets":[]},"status":{}}}` | Optional date range, asset, status, and department scope. | Forbidden scope, invalid query. | `200`, `400`, `401`, `403` | `bookings`, `assets`, `departments` |
| `GET /reports/maintenance` | Return maintenance repair time, frequency, cost, downtime, status/priority charts, and trend. | `Authorization` | Admin global; Manager department scoped; Employee reported/assigned tickets; Auditor read-only. | `?priority=HIGH&departmentId=uuid` | `{"maintenanceCount":8,"averageRepairTimeHours":3.5,"charts":{"priority":{"labels":[],"datasets":[]}}}` | Optional date range, priority, status, asset, and department scope. | Forbidden scope, invalid query. | `200`, `400`, `401`, `403` | `maintenance_tickets`, `assets`, `departments`, `users` |
| `GET /reports/audits` | Return audit completion, compliance, verification charts, damage/missing stats, and discrepancy summaries. | `Authorization` | Admin global; Manager department scoped; Employee assigned assets; Auditor read-only. | `?departmentId=uuid` | `{"auditCount":4,"completionPercentage":75,"charts":{"verification":{"labels":[],"datasets":[]}}}` | Optional department, result, audit status, asset, auditor, and date filters. | Forbidden scope, invalid query. | `200`, `400`, `401`, `403` | `audits`, `audit_records`, `audit_discrepancies`, `assets`, `departments` |
| `GET /reports/utilization` | Return global/scoped asset and booking utilization percentages. | `Authorization` | Admin global; Manager department scoped; Employee own data; Auditor read-only. | `?departmentId=uuid` | `{"totals":{"assetUtilizationPercentage":40},"charts":{"utilizationSummary":{"labels":["Allocated"],"datasets":[]}}}` | Valid date range and scope. | Forbidden scope, invalid query. | `200`, `400`, `401`, `403` | `assets`, `allocations`, `bookings`, `departments` |
| `GET /reports/department-utilization` | Return utilization rows and chart data by department. | `Authorization` | Admin global; Manager assigned department; Employee own accessible departments; Auditor read-only. | `?departmentId=uuid` | `{"departmentUtilization":[{"department":{},"totalAssets":5,"utilizationPercentage":40}],"charts":{"utilizationByDepartment":{}}}` | Valid department scope. | Forbidden scope, invalid UUID. | `200`, `400`, `401`, `403` | `departments`, `assets`, `bookings` |
| `GET /reports/idle-assets` | Return available assets with no active allocation, booking, or maintenance work. | `Authorization` | Admin global; Manager department scoped; Employee own data; Auditor read-only. | `?page=1&limit=10` | `{"items":[{"assetCode":"FUR-001","idleDays":3,"activityCounts":{}}],"total":1}` | Valid pagination and scope. | Forbidden scope. | `200`, `400`, `401`, `403` | `assets`, `allocations`, `bookings`, `maintenance_tickets` |
| `GET /reports/most-used-assets` | Return ranked assets by allocation and booking usage. | `Authorization` | Admin global; Manager department scoped; Employee own data; Auditor read-only. | `?limit=5` | `{"items":[{"assetCode":"LAP-001","usage":{"totalUsage":4}}],"charts":{"mostUsedAssets":{"labels":[],"datasets":[]}}}` | Valid pagination and scope. | Forbidden scope. | `200`, `400`, `401`, `403` | `assets`, `allocations`, `bookings`, `maintenance_tickets` |
| `GET /reports/near-retirement` | Return assets with retirement risk derived from condition, warranty expiry, and age. | `Authorization` | Admin global; Manager department scoped; Employee own data; Auditor read-only. | `?limit=5` | `{"items":[{"assetCode":"MON-001","riskScore":55,"reasons":["Condition is fair."]}],"charts":{"retirementRisk":{}}}` | Valid pagination and scope. | Forbidden scope. | `200`, `400`, `401`, `403` | `assets`, `categories`, `departments` |
| `GET /reports/export` | Export any report as JSON or CSV; return a future-ready PDF interface response. | `Authorization` | Admin global; Manager department scoped; Employee own data; Auditor read-only. | `?type=assets&format=csv` | CSV response for `format=csv`; JSON wrapper for `format=json`; `{"format":"pdf","data":{"status":"PDF_INTERFACE_READY"}}` for PDF. | `type` must be a supported report type; `format` must be `json`, `csv`, or `pdf`. | Forbidden scope, invalid type/format. | `200`, `400`, `401`, `403` | Varies by selected report |
| `GET /notifications` | List notification-center items with filters, pagination, sorting, unread count, and role scope. | `Authorization` | Admin global; Manager department notifications; Employee own notifications; Auditor read-only global. | `?status=unread&type=BOOKING&priority=HIGH&page=1&limit=20&sortBy=createdAt&sortOrder=desc` | `{"items":[{"id":"ntf_1","category":"BOOKING","priority":"HIGH","status":"unread","recipient":{}}],"unreadCount":4}` | Valid status, type/category, priority, search, date range, pagination, and sorting. | Missing token, invalid query. | `200`, `400`, `401` | `notifications`, `users` |
| `GET /notifications/unread-count` | Return unread count and badge count for the current role scope. | `Authorization` | Admin global; Manager department notifications; Employee own notifications; Auditor read-only global. | None | `{"unreadCount":4,"notificationBadgeCount":4}` | Valid JWT required. | Missing token. | `200`, `401` | `notifications` |
| `GET /notifications/:id` | Fetch one notification detail if visible to the current role. | `Authorization` | Same read scope as notification list. | None | `{"id":"ntf_1","title":"Transfer approved","status":"unread","recipient":{}}` | Valid notification UUID. | Not found or outside scope. | `200`, `400`, `401`, `404` | `notifications`, `users` |
| `PATCH /notifications/:id/read` | Mark one visible notification as read without changing any business workflow state. | `Authorization` | Admin global; Manager department notifications; Employee own notifications. Auditor read-only. | None | `{"id":"ntf_1","isRead":true,"readAt":"..."}` | Notification must be visible to actor. | Not found, read-only auditor. | `200`, `400`, `401`, `403`, `404` | `notifications` |
| `PATCH /notifications/read-all` | Atomically mark all unread notifications in the actor's scope as read. | `Authorization` | Admin global; Manager department notifications; Employee own notifications. Auditor read-only. | None | `{"updated":3}` | Valid JWT required. | Missing token, read-only auditor. | `200`, `401`, `403` | `notifications` |
| `DELETE /notifications/:id` | Delete the selected notification row from the visible notification center. Since notifications are per-recipient rows, this removes only that user's copy. | `Authorization` | Admin global; Manager department notifications; Employee own notifications. Auditor read-only. | None | `{"id":"ntf_1","title":"Transfer approved"}` | Notification must be visible to actor. | Not found, read-only auditor. | `200`, `400`, `401`, `403`, `404` | `notifications` |

### System Settings

Settings endpoints are mounted under `/api/settings`. Organization and policy settings are stored in `system_settings`; profile preferences are stored per user in `user_preferences`. Permission inspection is read-only and reflects the backend RBAC contract.

| Endpoint | Purpose | Headers | Authorization | Example Request | Example Response | Validation | Possible Errors | HTTP Codes | Related Tables |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `GET /settings/company` | Return company identity and organization preferences. | `Authorization` | Admin, Manager, Auditor. | None | `{"companyName":"AssetFlow Demo Organization","timezone":"Asia/Kolkata","currency":"INR","workingHours":{"start":"09:00","end":"18:00"}}` | Valid JWT and allowed role. | Missing token, forbidden role. | `200`, `401`, `403` | `system_settings` |
| `PATCH /settings/company` | Update company name, logo URL, address, timezone, currency, language, working hours, and contact details. | `Content-Type`, `Authorization` | Admin. | `{"companyName":"AssetFlow HQ","currency":"INR","workingHours":{"start":"09:30","end":"18:30"}}` | `{"companyName":"AssetFlow HQ","currency":"INR","updatedById":"usr_1"}` | Currency must be a 3-letter code; URLs, email, and working hours must be valid. | Invalid payload, forbidden role. | `200`, `400`, `401`, `403` | `system_settings`, `audit_logs` |
| `GET /settings/profile` | Return current user's profile preferences. | `Authorization` | Any authenticated user. | None | `{"userId":"usr_1","displayName":"AssetFlow Admin","theme":"SYSTEM","notificationPreferences":{"email":true,"inApp":true}}` | Valid JWT required. | Missing token. | `200`, `401` | `users`, `departments`, `user_preferences` |
| `PATCH /settings/profile` | Update current user's avatar, display name, theme, timezone, language, and notification preferences. | `Content-Type`, `Authorization` | Any authenticated user. | `{"theme":"DARK","notificationPreferences":{"criticalOnly":true,"muteCategories":["BOOKING"]}}` | `{"userId":"usr_1","theme":"DARK","notificationPreferences":{"criticalOnly":true}}` | Theme must be `LIGHT`, `DARK`, or `SYSTEM`; notification categories must be supported. | Invalid payload, missing token. | `200`, `400`, `401` | `user_preferences`, `audit_logs` |
| `GET /settings/roles` | Return role metadata used by the settings screen. | `Authorization` | Admin, Manager, Auditor. | None | `{"roles":[{"role":"ADMIN","label":"Admin","accessLevel":"GLOBAL"}],"editable":false}` | Valid JWT and allowed role. | Forbidden role. | `200`, `401`, `403` | Prisma `Role` enum |
| `GET /settings/permissions` | Return read-only permission matrix for backend modules. | `Authorization` | Admin, Manager, Auditor. | None | `{"roles":["ADMIN","MANAGER","EMPLOYEE","AUDITOR"],"matrix":[{"module":"Assets","admin":"FULL"}],"readOnly":true}` | Valid JWT and allowed role. | Forbidden role. | `200`, `401`, `403` | None |
| `GET /settings/asset-configuration` | Return asset tag, auto-numbering, QR default, depreciation, and retirement-threshold configuration. | `Authorization` | Admin, Manager, Auditor. | None | `{"assetTagPrefix":"AST","autoNumbering":true,"qrDefaults":{"enabled":true},"defaultDepreciationYears":5}` | Valid JWT and allowed role. | Forbidden role. | `200`, `401`, `403` | `system_settings` |
| `PATCH /settings/asset-configuration` | Update asset configuration. | `Content-Type`, `Authorization` | Admin. | `{"assetTagPrefix":"AST","autoNumbering":true,"retirementThresholdPercent":80}` | `{"assetTagPrefix":"AST","autoNumbering":true}` | Prefix must use uppercase letters, numbers, or hyphen; numeric limits must be valid. | Invalid payload, forbidden role. | `200`, `400`, `401`, `403` | `system_settings`, `audit_logs` |
| `GET /settings/booking-policies` | Return booking policy defaults. | `Authorization` | Admin, Manager, Auditor. | None | `{"maxBookingDurationHours":8,"requireApproval":true,"advanceBookingLimitDays":30,"allowWeekendBookings":false}` | Valid JWT and allowed role. | Forbidden role. | `200`, `401`, `403` | `system_settings` |
| `PATCH /settings/booking-policies` | Update booking policy defaults. | `Content-Type`, `Authorization` | Admin. | `{"maxBookingDurationHours":4,"requireApproval":true,"allowWeekendBookings":false}` | `{"maxBookingDurationHours":4,"requireApproval":true}` | Duration cannot exceed the advance booking window; business hours must be valid. | Invalid payload, forbidden role. | `200`, `400`, `401`, `403` | `system_settings`, `audit_logs` |
| `GET /settings/maintenance-policies` | Return maintenance policy defaults. | `Authorization` | Admin, Manager, Auditor. | None | `{"defaultTechnicianId":"usr_2","autoAssignmentEnabled":false,"allowedPriorities":["LOW","MEDIUM","HIGH","CRITICAL"],"escalationDays":3}` | Valid JWT and allowed role. | Forbidden role. | `200`, `401`, `403` | `system_settings`, `users` |
| `PATCH /settings/maintenance-policies` | Update maintenance policy defaults. | `Content-Type`, `Authorization` | Admin. | `{"defaultTechnicianId":"usr_2","autoAssignmentEnabled":true,"escalationDays":2}` | `{"defaultTechnicianId":"usr_2","autoAssignmentEnabled":true}` | Default technician must exist and be an active non-auditor user. | Invalid payload, missing technician, invalid technician role/status. | `200`, `400`, `401`, `403`, `404`, `409` | `system_settings`, `users`, `audit_logs` |

## Pagination

List endpoints should support:

| Query Parameter | Purpose |
| --- | --- |
| `page` | Page number starting at `1`. |
| `limit` | Page size. |
| `search` | Text search where supported. |
| `status` | Status filter. On notifications this supports `read`, `unread`, and `all`. |
| `role` | User role filter on `GET /users`. |
| `departmentId` | Department filter on users, assets, and reports. |
| `parentDepartmentId` | Parent department filter on `GET /departments`. |
| `result` | Audit result filter on `GET /audits`. |
| `priority` | Maintenance priority filter on `GET /maintenance`; notification priority filter on `GET /notifications` using `INFO`, `LOW`, `MEDIUM`, `HIGH`, or `CRITICAL`. |
| `entityType` and `entityId` | Audit-log entity filters. |
| `from` and `to` | Date range filter. |
| `type` | Report type selector on `GET /reports/export`, or notification category/detail type on `GET /notifications`. Notification categories include `BOOKING`, `ALLOCATION`, `TRANSFER`, `MAINTENANCE`, `AUDIT`, `SYSTEM`, `APPROVAL`, and `ASSET`. |
| `format` | Export format on `GET /reports/export`. Supported values are `json`, `csv`, and `pdf`. |
| `sortBy` and `sortOrder` | Sorting controls where supported. Notifications support `createdAt`, `updatedAt`, `title`, `type`, and `priority`. |

## Error Codes

| Code | Meaning |
| --- | --- |
| `UNAUTHORIZED` | Missing or invalid JWT. |
| `FORBIDDEN` | Role or scope does not allow action. |
| `VALIDATION_ERROR` | Request data is invalid. |
| `NOT_FOUND` | Resource does not exist. |
| `CONFLICT` | Request conflicts with current data. |
| `INTERNAL_ERROR` | Unexpected server failure. |
